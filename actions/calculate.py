def calculate(what):
    """
    Evaluate a string as a Python expression and return the result.

    Args:
        what: A string to evaluate as a Python expression.

    Returns:
        The result of evaluating the expression.
    """
    return eval(what)
